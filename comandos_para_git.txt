git --version

config --global user.name "Carlos Rocha"

git config user.name

git config --global user.email "carlosrocha0194@gmail.com"

git config user.email

git config --global color.ui true

###		Crear un nuevo repositorio		###

git init

git status # se revisa el estatus de los archivos en el repositorio

git add index.html # se agregan los archivos para hacer committed 

git commit -m "Agregando index.html" # se hace committed de los archivos